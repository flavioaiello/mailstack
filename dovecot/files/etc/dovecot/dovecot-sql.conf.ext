driver = sqlite
connect = /data/main.db

# User password
password_query = SELECT password FROM user WHERE user.email = '%u'

# User quota
quota_query = SELECT '*:bytes=' || user.quota_bytes AS quota_rule FROM user WHERE user.email = '%u'
